/**
 * This file defines the structure of a single Todo item, with fields for 'id', 'text', and 'isComplete'.
 */
type TodoItem = {
  id: number;
  text: string;
  isComplete: boolean;
};

export default TodoItem;
